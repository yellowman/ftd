#!/bin/ksh
# ftd FastCGI form collector
#
# All configuration lives in /etc/ftd.conf, loaded by the daemon itself, so
# this script needs no environment handling.
daemon="/usr/local/bin/ftd"
daemon_flags=${ftd_flags:-""}

. /etc/rc.d/rc.subr

rc_bg=YES
rc_reload=NO

rc_cmd $1
